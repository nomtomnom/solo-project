import React from 'react';

function Graph() {

  // look into graph-making libraries?
  // SVG support?
  // alternate styles of graphs for different metrics 
  // (not everything wants a line graph)
  return (
    <div>
      <p>Graph</p>
    </div>
  );
}

export default Graph;